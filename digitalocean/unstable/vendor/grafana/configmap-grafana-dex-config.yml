apiVersion: v1
data:
  GF_AUTH_GENERIC_OAUTH_API_URL: https://auth.do.e2e.karavel.io/userinfo
  GF_AUTH_GENERIC_OAUTH_AUTH_URL: https://auth.do.e2e.karavel.io/auth
  GF_AUTH_GENERIC_OAUTH_ENABLED: "true"
  GF_AUTH_GENERIC_OAUTH_SCOPES: openid profile email groups
  GF_AUTH_GENERIC_OAUTH_TOKEN_URL: https://auth.do.e2e.karavel.io/token
kind: ConfigMap
metadata:
  name: grafana-dex-config
  namespace: monitoring
